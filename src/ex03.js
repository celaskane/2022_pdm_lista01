/*3. Crie um objeto JSON que abriga todas as operações do exercício 2.*/

const f = {
    soma: (x,y) => x+y,
    subtracao: (x,y) => x-y,
    multiplicacao: (x,y) => x*y,
    divisao: (x,y) => x/y
};